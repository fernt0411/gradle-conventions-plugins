plugins {
    id("conventionPlugin.android.application")
}

android {
    namespace = "com.example.convetionsplugins"
    defaultConfig {
        applicationId = "com.example.convetionsplugins"
    }
}
